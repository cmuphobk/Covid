# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Covid' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Covid
  pod 'SwiftCSV'

  target 'CovidTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'CovidMacOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Covid
  pod 'SwiftCSV'

  target 'CovidMacOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
